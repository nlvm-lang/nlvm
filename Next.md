# Next

## Questions

* Should we add an `nlc` option to disassemble a `.nlm` module / `.nlp` program?
* What should NL's interoperability story with other languages look like?
* Should we support transpiling NL code to another language?
* Should we provide a minimal LSP (real-time errors, go-to-definition) for editor integration?
* How should multi-file projects and third-party package management work?
* At what point does the bytecode interpreter become a bottleneck, and is a JIT or AOT compilation in scope?
* Should we write a user-facing "getting started" guide beyond the formal specs?
* Should we provide a REPL for interactive exploration?
* Should we add an internal code formater?
