---
title: "string.charAt index out of range surfaces as an unhandled IndexOutOfBoundsException"
file_separator: "#NLFILE"
expected_exit_code: 1
expected_stdout: ""
expected_stderr: "Unhandled exception: IndexOutOfBoundsException: index 5, length 5\n\tat phase6/charat_oob/Main.nl:5"
# optimizations.md § Testing: stderr carries a stack trace,
# so the -O0 and -O1 runs are not compared with each other. Both still
# have to satisfy every expectation above.
optimization_sensitive: true
---

#NLFILE phase6/charat_oob/Main.nl
namespace phase6.charat_oob;
class Main {
	public static int main(string[] args) {
		string text = "Hello";
		string c = text.charAt(5);
		system.Out.print(c);
		return 0;
	}
}
